SQL data for phpMyAdmin demo server
===================================

This collects various data used on phpMyAdmin demo server.

Installation:

- ``run-mysql`` creates all databases
- ``privileges.sql`` creates needed users and grants
