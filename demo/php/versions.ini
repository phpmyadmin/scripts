#
# The demo server configuration
#
# It will get updated within one hour from commiting changes.

[demo]
# List of branches to show, master must come first.
# The special cases like master-* have just different configuration.
branches[] = master
branches[] = master-config
branches[] = master-http
branches[] = master-config-nopmadb
branches[] = STABLE
branches[] = QA_4_2
branches[] = MAINT_4_2_4

# Version of future release from master branch
master-release = 4.3.0
